构建入口为 `cargo build -p aether-win32 --bin aether-app`（debug/release），CLI 通过 `-p aether-cli --bin aether`；全量单元测试需先设置 `$env:CARGO_INCREMENTAL = '0'` 再执行 `cargo test --workspace --no-fail-fast`；GUI 冒烟测试需先 release 构建再运行 `python tests/gui_smoke.py`；覆盖率收集使用 `powershell -File tests/tools/coverage.ps1`。