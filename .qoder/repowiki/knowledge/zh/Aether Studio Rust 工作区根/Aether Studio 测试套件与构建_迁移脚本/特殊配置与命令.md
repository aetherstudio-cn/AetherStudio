统一入口 `pwsh -File tests/run_tests.ps1 -Suite {unit|gui|ai|coverage|all} [-Case <name>] [-SkipBuild]`；GUI 用例会启动真实 `aether-app.exe` 并模拟鼠标键盘，运行期间不可操作桌面；覆盖率通过 `tools/coverage.ps1` 配合 llvm-cov 生成；发布构建使用 `scripts/build-release.ps1 [-Release] [-Run]`。