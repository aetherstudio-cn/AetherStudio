PowerShell (pwsh) + .NET `System.Drawing`/`ProcessStartInfo` 实现 Windows GUI 自动化；通过内联 C# 调用 `user32.dll` 的 `PostMessageW`/`mouse_event`/`GetWindowRect` 等 Win32 API 注入鼠标键盘消息；Rust/Cargo 作为被测应用与单元测试后端；Python 遗留脚本 (`legacy/`) 仅作存档。